#include <bits/stdc++.h>
using namespace std;

int main() {
    // float num = (1/2) * 100 * (100+1);

    // cout << 0.5 * 100 * (100+1) << endl;

    cout << 100 * (100+1) /2 << endl; // 最後に/2をすることで想定の答えが得られる

    // メモ
    // C++では、整数同士で割り算した場合、結果は小数点以下を切り捨てした値になります。
    // (7÷3=2.33...→2, -7÷3=-2.33...→-2)
    // 小数点以下を切り捨てないで計算してほしい場合、7.0 / 3.0のように.0をつけます。同様に7.0 / 3.5のような計算も行えます。)

}
