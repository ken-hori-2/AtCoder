#include <bits/stdc++.h>
using namespace std;
#include <array>
#include <vector>

#define SIZE_OF_ARRAY(results_data)    results_data.size()  // 3
// #define MATCH "True"
#define Guidance "Hop Step Jump !"
#define LINE "********************"


namespace apps {
  namespace sensor {
    int test(int num){
        
        num += 100;

        return num;

    }
  }
}
