#include <bits/stdc++.h>
using namespace std;
#include <array>
#include <vector>


namespace apps {
    namespace sensor {

        bool demo_func(std::vector<int>& results_data);
        
        int test(int num);
    }
}